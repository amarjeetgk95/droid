'use client';

export { executeEmergencyKill } from '@/lib/api/algo';

/**
 * AlgoControlWidget — legacy adapter.
 * The unrendered component body has been retired; emergency kill switch logic
 * now lives in `@/lib/api/algo`.
 */
export const AlgoControlWidget = () => null;
export default AlgoControlWidget;
