'use client';

// Backwards-compatibility shim re-exporting the modular Header components
export { Header as TopHeader, loadTickerVisible, saveTickerVisible } from './Header';
export type { HeaderProps } from './Header';
export * from './Header';
